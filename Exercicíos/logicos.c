#include <stdio.h>
/*
    Operador E - &&
    Operador OU - ||
*/
int main() {
    int x;
    scanf("%d", &x);

    if (x%2 == 0) {
        if(x>0) {
            printf("Par e positivo\n");
        }
    }
}
